{
	"name": "file-folder-highlighter",
	"version": "1.3.6",
	"description": "Custom colors for files/folders via right-click, YAML frontmatter rules, hierarchy, regex, and conditional highlighting.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc --noEmit -p tsconfig.json && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"lint": "eslint src",
		"format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
		"format:check": "prettier --check \"src/**/*.ts\" \"test/**/*.ts\"",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage"
	},
	"keywords": [],
	"author": "Josh",
	"license": "MIT",
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@eslint/json": "^2.1.0",
		"@types/node": "^26.4.1",
		"@vitest/coverage-v8": "^4.1.11",
		"esbuild": "0.28.2",
		"eslint": "^10.9.1",
		"eslint-config-prettier": "^10.1.8",
		"eslint-plugin-obsidianmd": "^0.4.2",
		"obsidian": "latest",
		"prettier": "^3.9.6",
		"tslib": "2.8.1",
		"typescript": "^7.0.2",
		"typescript-eslint": "^8.61.0",
		"vitest": "^4.1.8"
	}
}
